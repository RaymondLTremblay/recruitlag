% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ceiling.R
\name{lag_ceiling}
\alias{lag_ceiling}
\title{The ceiling that any delay places on the concentration of recruitment}
\usage{
lag_ceiling(
  X,
  R = NULL,
  t_R = NULL,
  K,
  lag0 = 1L,
  kernels = lag_kernels(K, n_dirichlet = 20000L),
  missing = c("backfill", "drop"),
  period = "period",
  reproduction = "reproduction",
  recruits = "recruits"
)
}
\arguments{
\item{X}{Either a numeric vector, the reproductive record at periods
\verb{1, 2, ...}, or a data frame with one row per period (or per unit and
period, which is summed) holding the columns named in \code{period},
\code{reproduction} and \code{recruits}; recruits are \code{NA} at unscored periods.}

\item{R}{Numeric vector: recruits scored at the periods \code{t_R}. Ignored
when \code{X} is a data frame.}

\item{t_R}{Periods of \code{X} at which \code{R} was scored; defaults to the last
\code{length(R)} periods of \code{X}. Ignored when \code{X} is a data frame.}

\item{K}{Lag horizon (see \code{\link[=lag_kernels]{lag_kernels()}}).}

\item{lag0}{Offset of the first bin (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{kernels}{A \code{lag_kernels} object. Defaults to every family with
20,000 Dirichlet draws, as in the companion paper.}

\item{missing}{Treatment of pre-record lags (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{period, reproduction, recruits}{Column names used when \code{X} is a data
frame.}
}
\value{
An object of class \code{lag_ceiling} with elements \code{obs} (the
concentration of \code{R}), \code{theorem} (the concentration of \code{X}, which is the
bound), \code{ceiling} (the largest Gini and CV any searched kernel produced
on the periods \code{t_R}), \code{exceedance} (observed over ceiling), \code{best} (the
kernel attaining the ceiling), \code{search} (a data frame with one row per
kernel) and the inputs.
}
\description{
If recruitment is the delayed consequence of reproduction, expected
recruitment is a convolution of the reproductive record, and a convolution
is a weighted average. A weighted average is never more concentrated in
time than the series it averages, so reproduction's own concentration is a
ceiling on the concentration of expected recruitment, attained only by a
pure delay. \code{lag_ceiling()} states that bound, searches a family of lag
weights to confirm that none exceeds it on the periods at which recruits
are scored, and compares the observed recruitment series with it.
}
\details{
The searched ceiling can differ from the theorem bound by a small
amount in either direction, because the kernels are scored on the
recruitment periods only and pre-record lags are backfilled. The
exceedance bounds the expected series, not the counts: whether a given
exceedance refutes the delay depends on the sampling variation of the
counts, which \code{\link[=ceiling_calibration]{ceiling_calibration()}} evaluates for the pooled series and
\code{\link[=host_lag_test]{host_lag_test()}} evaluates across replicate units.
}
\examples{
ce <- lag_ceiling(lepanthes_census, reproduction = "inflorescences", K = 4,
                  kernels = lag_kernels(4, n_dirichlet = 200, bin = 6, unit = "mo"))
ce
# the same from vectors
d <- lepanthes_census
lag_ceiling(d$inflorescences, d$recruits[3:12], t_R = 3:12, K = 4,
            kernels = lag_kernels(4, bin = 6, unit = "mo"))
}
