% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forest.R
\name{ceiling_draws_table}
\alias{ceiling_draws_table}
\title{One table for several records and routes}
\usage{
ceiling_draws_table(..., .list = NULL, quantity = "exceedance")
}
\arguments{
\item{...}{\code{lag_ceiling_draws} objects, named. The names become the
\code{record} column, so name them by the record or the route as the table
needs: \code{`L. eltoroensis` = b} or \code{`L. eltoroensis, bootstrap` = b}.}

\item{.list}{Alternatively, a named list of such objects.}

\item{quantity}{Which rows to keep: \code{"exceedance"} (the default), \code{"all"},
or any of the six quantity labels (\code{"observed Gini"}, \code{"ceiling Gini"},
\code{"exceedance Gini"}, \code{"observed CV"}, \code{"ceiling CV"}, \code{"exceedance CV"}).}
}
\value{
A data frame with columns \code{record}, \code{method}, \code{estimand},
\code{quantity}, \code{estimate}, \code{lower}, \code{upper}, \code{level}, \code{interval},
\code{probability}, \code{probability_is} and \code{note}. \code{probability} is filled on
the exceedance rows only.
}
\description{
Stacks the interval tables of any number of \code{lag_ceiling_draws} objects
(from \code{\link[=lag_ceiling_boot]{lag_ceiling_boot()}}, \code{\link[=lag_ceiling_bayesboot]{lag_ceiling_bayesboot()}} or
\code{\link[=lag_ceiling_stan]{lag_ceiling_stan()}}) into one tidy data frame with a \code{record} column, so
that a document can print one table and a reader can compare records and
routes down a column. The probability statement each route makes is
carried in its own column with its name beside it, because the bootstrap's
one-sided p-value and the Bayesian routes' posterior probability are
different things and must not share a heading.
}
\section{Scale and how to read it}{


\code{estimate}, \code{lower} and \code{upper} are on the scale of the quantity: a
concentration index for the observed and ceiling rows, a ratio centred on
1 for the exceedance rows. \code{interval} says which interval it is (BCa,
percentile, equal-tailed, highest-density) and \code{method} which route made
it, and the two together fix how the interval may be read: a confidence
interval from the cluster bootstrap, a credible interval from the other
two. \code{probability_is} names the statement in \code{probability}. Print the
table with \code{knitr::kable(digits = 2)} in a document.
}

\examples{
set.seed(1)
k <- lag_kernels(4, bin = 6, unit = "mo")
b  <- lag_ceiling_boot(lepanthes_hosts, unit = "host", reproduction = "inflorescences",
                       K = 4, kernels = k, R = 200)
bb <- lag_ceiling_bayesboot(lepanthes_hosts, unit = "host", reproduction = "inflorescences",
                            K = 4, kernels = k, draws = 200)
ceiling_draws_table(`L. eltoroensis` = b, `L. eltoroensis` = bb)
}
