% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{host_matrices}
\alias{host_matrices}
\title{Build the unit-by-period matrices from a long data frame}
\usage{
host_matrices(
  data,
  unit = "unit",
  period = "period",
  reproduction = "reproduction",
  recruits = "recruits"
)
}
\arguments{
\item{data}{A data frame with one row per unit and period.}

\item{unit, period, reproduction, recruits}{Names of the columns holding the
unit identifier, the period index (integer, from 1), the reproductive
count and the recruit count.}
}
\value{
A list with \code{R} (recruits, units by scored periods, \code{colnames} the
period indices) and \code{X} (reproduction, units by periods 1 to the last
period in \code{data}). Missing reproduction is treated as zero.
}
\description{
The host-level functions work on two matrices, recruits (units by scored
periods) and reproduction (units by all periods). Most census data live in
a long data frame with one row per unit and period; this function makes
the matrices from it. Periods at which \code{recruits} is \code{NA} for every unit
are taken as unscored.
}
\examples{
m <- host_matrices(lepanthes_hosts, unit = "host", reproduction = "inflorescences")
dim(m$R); dim(m$X)
}
