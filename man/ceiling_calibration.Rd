% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{ceiling_calibration}
\alias{ceiling_calibration}
\title{Calibrate the pooled ceiling comparison against realised counts}
\usage{
ceiling_calibration(x, vmr = NULL, nsim = 20000L)
}
\arguments{
\item{x}{A \code{lag_ceiling} object.}

\item{vmr}{Variance-to-mean ratio of the counts, used to set the
negative-binomial size parameter through \code{phi = mu / (vmr - 1)}.
Defaults to the variance-to-mean ratio of the observed recruit series.}

\item{nsim}{Number of simulated series per null.}
}
\value{
An object of class \code{ceiling_calibration}: a data frame with one
row per null (\verb{N0 flat mean}, \verb{N1 best-kernel mean}) giving \code{phi}, the
median and 5 to 95\% range of the simulated Gini and CV, and the
probabilities \code{p_gini}, \code{p_cv} and \code{p_zeros} that a simulated series is
at least as concentrated, or has at least as many zero periods, as the
observed one. The simulated draws are kept in \code{attr(, "sims")} for
plotting.
}
\description{
The ceiling bounds the expected recruitment series. Observed recruits are
counts about that expectation, and small clumped counts look episodic
whatever their expectation. \code{ceiling_calibration()} simulates
negative-binomial counts about two means that both obey the ceiling, a flat
mean (N0) and the best-kernel mean rescaled to the observed total (N1), and
reports how often the simulated series is at least as concentrated as the
observed one. In the companion paper this shows that the pooled comparison
has no power on its own; the host-level test (\code{\link[=host_lag_test]{host_lag_test()}}) is where
the refutation is made.
}
\examples{
ce <- lag_ceiling(lepanthes_census, reproduction = "inflorescences", K = 4,
                  kernels = lag_kernels(4, bin = 6, unit = "mo"))
ceiling_calibration(ce, nsim = 2000)
}
