% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/by_unit.R
\name{lag_ceiling_by}
\alias{lag_ceiling_by}
\title{The ceiling for every unit in a long data frame}
\usage{
lag_ceiling_by(
  data,
  unit = "unit",
  period = "period",
  reproduction = "reproduction",
  recruits = "recruits",
  K,
  lag0 = 1L,
  kernels = lag_kernels(K),
  missing = c("backfill", "drop"),
  min_periods = K + 3L,
  min_recruits = 2
)
}
\arguments{
\item{data}{A data frame with one row per unit and period.}

\item{unit, period, reproduction, recruits}{Column names.}

\item{K}{Lag horizon (see \code{\link[=lag_kernels]{lag_kernels()}}).}

\item{lag0}{Offset of the first bin (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{kernels}{A \code{lag_kernels} object, used for every unit so that the
units are compared on the same family.}

\item{missing}{Treatment of pre-record lags (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{min_periods}{Units with fewer scored periods than this are skipped.
The default \code{K + 3} is the shortest record on which a horizon of \code{K + 1}
bins leaves anything to compare.}

\item{min_recruits}{Units with fewer recruits than this are skipped, since
a unit with one recruit has concentration 1 by arithmetic rather than by
biology.}
}
\value{
A named list of \code{lag_ceiling} objects, of class
\code{lag_ceiling_list}, with the skipped units and the reason in
\code{attr(, "skipped")}. It can be passed straight to
\code{\link[=rain_strip]{rain_strip()}}, and \code{as.data.frame()} gives one row per unit.
}
\description{
Runs \code{\link[=lag_ceiling]{lag_ceiling()}} once per replicate unit (population, plot, host tree)
and returns the results together, so that a figure of one strip per unit,
or a table of one row per unit, is a single call. This is the comparison
the ceiling is usually wanted for: not whether one record exceeds its
bound, but how far above the bound each unit sits and how much that varies
within a study.
}
\details{
Rows in which both reproduction and recruits are missing are
taken as periods in which that unit was not watched, and are dropped
before its periods are re-indexed to start at 1. A unit followed from
2015 to 2021 is therefore a seven-period record, not a 22-period record
with fifteen silent years in front of it. Keeping them would read those
years as periods with no reproduction, which raises the unit's ceiling
and makes the test look weaker than it is. Units are otherwise
independent: nothing is pooled, and the kernels are the same for all of
them.
}
\section{Scale and how to read it}{


The columns of \code{as.data.frame()} carry the scales of \code{\link[=lag_ceiling]{lag_ceiling()}}:
\code{ceiling} and \code{observed} are concentration indices bounded above by
\eqn{(n-1)/n} for the Gini, where \code{n} is that unit's own number of scored
periods, and \code{exceedance} is their ratio, centred on 1.

Because \code{n} differs between units, two cautions apply to reading the table
or the figure down a column. A unit with few periods has a lower attainable
maximum, so its Gini is not on the same footing as a longer unit's, and
\code{n} is reported for that reason. And the exceedance is a ratio of two
numbers computed on the same \code{n}, so it is the safer quantity to compare
across units of different lengths.

What varies between units is usually the ceiling rather than the
observation: the ceiling is a property of that unit's reproductive record,
so a unit with seasonal or patchy reproduction sits higher and has less to
detect. A column of exceedances close to 1 is not a weak result, it is a
statement that those units' reproduction is itself episodic.
}

\examples{
ces <- lag_ceiling_by(lepanthes_hosts, unit = "host",
                      reproduction = "inflorescences", K = 4,
                      kernels = lag_kernels(4, bin = 6, unit = "mo"))
ces
as.data.frame(ces)
}
