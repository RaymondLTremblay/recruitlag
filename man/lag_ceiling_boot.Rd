% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{lag_ceiling_boot}
\alias{lag_ceiling_boot}
\title{A confidence interval for the exceedance, by resampling units}
\usage{
lag_ceiling_boot(
  data,
  unit = "unit",
  period = "period",
  reproduction = "reproduction",
  recruits = "recruits",
  K,
  lag0 = 1L,
  kernels = lag_kernels(K),
  missing = c("backfill", "drop"),
  R = 2000L,
  level = 0.9,
  type = c("bca", "percentile")
)
}
\arguments{
\item{data}{A data frame with one row per unit and period.}

\item{unit, period, reproduction, recruits}{Column names.}

\item{K}{Lag horizon (see \code{\link[=lag_kernels]{lag_kernels()}}).}

\item{lag0}{Offset of the first bin (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{kernels}{A \code{lag_kernels} object, used in every replicate. The
default is the named families with no random draws: the pure delays
attain the bound, so random kernels can only add time.}

\item{missing}{Treatment of pre-record lags (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{R}{Number of bootstrap replicates.}

\item{level}{Coverage of the interval. The default 0.9 matches the 90\%
credible intervals of the companion paper.}

\item{type}{\code{"bca"} (the default) for the bias-corrected and accelerated
interval of Efron (1987), with the acceleration from the leave-one-unit-
out estimates; \code{"percentile"} for the plain percentile interval.}
}
\value{
An object of class \code{lag_ceiling_draws} with \code{method = "bootstrap"}:
\code{point} (the six statistics on the full data, identical to
\code{\link[=lag_ceiling]{lag_ceiling()}} on the same kernels), \code{draws} (one row per replicate),
\code{table} (estimate and interval for each statistic), \code{p_boot} (the
one-sided bootstrap p-value for exceedance at most 1, on each index),
\code{n_units}, \code{R}, \code{n_failed} (replicates in which a statistic could not be
computed, usually because no resampled unit recruited) and the inputs.
}
\description{
The ceiling is arithmetic and carries no uncertainty, but the two
concentration indices it is compared with are estimated from a short
record, and so is their ratio, the exceedance. \code{lag_ceiling_boot()} puts
a confidence interval on them by resampling the replicate units (host
trees, plots, populations) with replacement, rebuilding \strong{both} the
reproductive series and the recruit series from the resampled units, and
recomputing the ceiling and the exceedance in every replicate. Numerator
and denominator therefore move together, as they should, since the same
units generate both series.
}
\section{Scale and how to read it}{


\strong{The interval is a confidence interval} and is read in the usual way:
the procedure covers the true value in \code{level} of repeated samples of
units from the same population of units. It is not a probability that the
exceedance lies in the interval. The Bayesian reading is available from
\code{\link[=lag_ceiling_bayesboot]{lag_ceiling_bayesboot()}}, which is built to give it.

\strong{\code{p_boot} is a one-sided bootstrap p-value}, the fraction of replicates
in which the exceedance was at most 1: the achieved significance level
against the hypothesis that the record is no more concentrated than a
delay predicts. It is not the probability that the exceedance is at most
1, although it is easily mistaken for one, and it cannot be resolved
below \code{1/R}. A value printed as \verb{< 1/R} means no replicate reached 1.

\strong{Why units and not periods.} The Gini and the CV are permutation
invariant, so resampling periods would not disturb them by reordering,
but periods within a unit are not independent and the units are the
design's replicates. Resampling units is the cluster bootstrap of Davison
and Hinkley (1997, ch. 3). With few units (the \emph{Lepanthes eltoroensis}
record has 23) the BCa interval is preferred to the percentile one, since
it corrects for the skew a ratio carries and for the bias between the
bootstrap distribution and the estimate.

\strong{What the interval does not say.} A wide interval does not weaken the
ceiling, which is a bound and not an estimate. It says how firmly the
record itself sits above the bound. Whether an exceedance above 1 refutes
the delay is still the question for \code{\link[=host_lag_test]{host_lag_test()}}, because the bound
applies to the expected series and not to the counts.
}

\examples{
set.seed(1)
b <- lag_ceiling_boot(lepanthes_hosts, unit = "host", reproduction = "inflorescences",
                      K = 4, R = 200, kernels = lag_kernels(4, bin = 6, unit = "mo"))
b
as.data.frame(b)
}
\references{
Davison, A. C. and Hinkley, D. V. (1997) \emph{Bootstrap Methods and their
Application}. Cambridge University Press, Cambridge.

DiCiccio, T. J. and Efron, B. (1996) Bootstrap confidence intervals.
\emph{Statistical Science} 11: 189-228. \doi{10.1214/ss/1032280214}

Efron, B. (1987) Better bootstrap confidence intervals. \emph{Journal of the
American Statistical Association} 82: 171-185.
\doi{10.1080/01621459.1987.10478410}
}
