% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{rain_strips}
\alias{rain_strips}
\title{Several strip figures instead of one unreadable one}
\usage{
rain_strips(
  x,
  group = NULL,
  size = 10,
  index = c("gini", "cv"),
  order_by = c("exceedance", "ceiling", "observed", "name")
)
}
\arguments{
\item{x}{A \code{lag_ceiling_list} from \code{\link[=lag_ceiling_by]{lag_ceiling_by()}}, or a named list of
\code{lag_ceiling} objects.}

\item{group}{Optional grouping for the units: either a vector as long as
\code{x}, or a named vector or one-to-one lookup whose names are unit names.
Units whose group is missing are collected as "ungrouped".}

\item{size}{Largest number of strips in one figure. Groups larger than this
are split into "... (1 of 3)" and so on.}

\item{index}{\code{"gini"} or \code{"cv"}, passed to \code{\link[=rain_strip]{rain_strip()}}.}

\item{order_by}{\code{"exceedance"} (default), \code{"ceiling"}, \code{"observed"} or
\code{"name"}: how units are ordered within each figure.}
}
\value{
A named list of ggplots. Print them in a loop, one per figure.
}
\description{
\code{\link[=rain_strip]{rain_strip()}} is readable up to about a dozen series. With more units than
that, \code{rain_strips()} splits them and returns one figure per group, either
by a grouping you supply (tree species, transect, site) or in chunks of
\code{size}. A group that is still too large is split further, and the figures
are named so the caption can say which is which.
}
\examples{
ces <- lag_ceiling_by(lepanthes_hosts, unit = "host",
                      reproduction = "inflorescences", K = 4,
                      kernels = lag_kernels(4, bin = 6, unit = "mo"))
figs <- rain_strips(ces, size = 8)
names(figs)
figs[[1]]
}
