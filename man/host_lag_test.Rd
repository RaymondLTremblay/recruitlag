% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/host_test.R
\name{host_lag_test}
\alias{host_lag_test}
\title{The host-level test of the lag hypothesis}
\usage{
host_lag_test(
  R,
  X = NULL,
  K,
  lag0 = 1L,
  kernels = lag_kernels(K, bin = NULL),
  missing = c("backfill", "renormalise", "zero"),
  flat = TRUE,
  phi = "moment",
  nsim = 10000L,
  unit = "unit",
  period = "period",
  reproduction = "reproduction",
  recruits = "recruits"
)
}
\arguments{
\item{R}{Either a data frame with one row per unit and census holding the
columns named in \code{unit}, \code{period}, \code{reproduction} and \code{recruits}
(recruits \code{NA} at unscored censuses; see \code{\link[=host_matrices]{host_matrices()}}), or a numeric
matrix of recruit counts, one row per unit and one column per census at
which recruits are scored, with \code{colnames(R)} the census indices and
\code{rownames(R)} the unit identifiers.}

\item{X}{Numeric matrix of the reproductive record, one row per unit and
one column per census from the first, with the same \code{rownames} as \code{R}.
Ignored when \code{R} is a data frame.}

\item{K}{Lag horizon (see \code{\link[=lag_kernels]{lag_kernels()}}).}

\item{lag0}{Offset of the first bin (see \code{\link[=convolve_lag]{convolve_lag()}}).}

\item{kernels}{A \code{lag_kernels} object; defaults to the named families
(pure delays, windows at every start, geometric decay) with no random
draws. Add \code{n_dirichlet} draws for a fuller search, at a cost in time.}

\item{missing}{Treatment of lags before the first column of \code{X}:
\code{"backfill"} reads them as the first census; \code{"renormalise"} drops them
and renormalises the remaining weights; \code{"zero"} reads them as zero. The
last two force a long delay to predict nothing at the first censuses and
are offered only as sensitivity cases.}

\item{flat}{Whether to add the flat-rate reference, in which each unit's
total is spread evenly over the censuses.}

\item{phi}{\code{"moment"} for the moment estimate under each profile, or a
numeric vector of fixed values at which to evaluate every profile as
well (a stress test with more clumping than the counts support).}

\item{nsim}{Simulated series per profile.}

\item{unit, period, reproduction, recruits}{Column names used when \code{R} is a
data frame.}
}
\value{
An object of class \code{host_lag_test} with \code{obs} (the observed
statistics of the census totals), \code{table} (one row per profile and phi
setting: \code{label}, \code{family}, \code{phi_source}, \code{phi}, \code{gini_expected} (the Gini
of the expected census totals), \code{p_silent}, \code{p_gini}, \code{p_cv}, \code{p_max},
\code{median_silent}), \code{verdict} (the maximum of each probability over
profiles, with the profile attaining it), and the inputs.
}
\description{
Tests whether the recruitment record, scored across replicate units, could
have arisen under a delayed consequence of each unit's own reproduction.
For each lag profile the expected counts are those of
\code{\link[=expected_recruits]{expected_recruits()}}, the clumping is \code{\link[=phi_moment]{phi_moment()}} (or a value you
supply), and negative-binomial counts are simulated for every unit and
summed over units to give a system-wide census series. The test reports,
for each profile, the probability that a simulated series is at least as
extreme as the observed one on each statistic. The verdict is the maximum
of that probability over every profile, that is, the most favourable case
the lag hypothesis can make for itself.
}
\details{
The statistics are: \code{silent}, the number of censuses at which no
unit recruited; \code{gini} and \code{cv}, the concentration of the census totals;
\code{max}, the largest census total. The Gini and CV are the pre-specified
statistics of the companion paper; the silent-census count is the most
interpretable and the largest census the least sensitive.
}
\section{Scale and how to read it}{


\strong{The probabilities.} \code{p_silent}, \code{p_gini}, \code{p_cv} and \code{p_max} are
proportions in [0, 1]: the fraction of \code{nsim} simulated records, drawn
under the profile in that row, that are at least as extreme as the
observed one on that statistic. They are predictive probabilities in the
sense of Gelman, Meng and Stern (1996), computed under a fully specified
null with nothing fitted. A value near 0.5 means the observed record is an
ordinary draw under that profile. A value near 0 means it is not.

\strong{The resolution floor.} A probability cannot be resolved below
\eqn{1/\mathrm{nsim}}{1/nsim}. With the default \code{nsim = 10000} a reported 0.0000
means "smaller than 1e-4", not zero, and the difference between 0.0000 and
0.0002 is one simulated record. Raise \code{nsim} before reading anything into
a very small value, and note that \code{\link[=plot.host_lag_test]{plot.host_lag_test()}} floors the axis
at \eqn{1/\mathrm{nsim}}{1/nsim} for the same reason.

\strong{The verdict is a maximum, not an average.} \code{verdict} takes, for each
statistic, the largest probability over every profile and every \code{phi}
setting, together with the profile attaining it. It is therefore the best
case the lag hypothesis can make for itself over the whole family
searched. A small verdict says that no profile in the family, not merely
the fitted one, reproduces the record.

\strong{No threshold.} The package applies no cutoff to these probabilities and
reports them as numbers. The 0.05 line drawn by \code{\link[=plot.host_lag_test]{plot.host_lag_test()}} is
a conventional reference mark and not a decision rule; a bright-line
threshold is exactly what the ASA statement on p-values advises against
(Wasserstein and Lazar 2016). What the companion paper reports is the
magnitude (a verdict of 0.0007 at the moment \code{phi}, 0.048 at a \code{phi} fixed
three times lower) and leaves the reader the judgement.

\strong{Where the test is weak.} The probabilities rise, correctly, when
reproduction is seasonal, because a concentrated input can produce a
concentrated output and the ceiling is then high. An ordinary verdict is
therefore not evidence for a lag; it is the absence of evidence against
one. See the \code{invented-regimes} vignette, which shows both outcomes on
constructed records whose mechanism is known.
}

\examples{
ht <- host_lag_test(lepanthes_hosts, unit = "host", reproduction = "inflorescences",
                    K = 4, nsim = 500, kernels = lag_kernels(4, bin = 6, unit = "mo"))
ht
}
\references{
Gelman, A., Meng, X.-L. and Stern, H. (1996) Posterior predictive
assessment of model fitness via realized discrepancies. \emph{Statistica
Sinica} 6: 733-760.

Wasserstein, R. L. and Lazar, N. A. (2016) The ASA statement on p-values:
context, process, and purpose. \emph{The American Statistician} 70: 129-133.
\doi{10.1080/00031305.2016.1154108}
}
