% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels.R
\name{lag_kernels}
\alias{lag_kernels}
\title{Families of lag weights}
\usage{
lag_kernels(
  K,
  families = c("delay", "window", "geometric", "dirichlet"),
  windows = c("all", "anchored"),
  rho = c(0.2, 0.4, 0.6, 0.8, 0.95),
  n_dirichlet = 0L,
  alpha = 0.3,
  bin = NULL,
  unit = "",
  extra = NULL
)
}
\arguments{
\item{K}{Lag horizon: the number of bins beyond the first, so that a kernel
has \code{K + 1} weights. The paper uses \code{K = 6} for monthly data (lags 0 to 6
months) and \code{K = 4} for six-monthly data (five bins, 6 to 30 months
before the recruit census).}

\item{families}{Which families to include. \code{"delay"}: pure delays, all
weight on one bin (these attain the ceiling). \code{"window"}: uniform windows
of every width from 2 to \code{K + 1}, at every start (\code{windows = "all"}) or
anchored at the first bin only (\code{windows = "anchored"}). \code{"geometric"}:
geometric decay at the rates in \code{rho}. \code{"dirichlet"}: \code{n_dirichlet}
random draws from a symmetric Dirichlet distribution with shape \code{alpha}.}

\item{windows}{\code{"all"} or \code{"anchored"}; see \code{families}.}

\item{rho}{Decay rates for the geometric family.}

\item{n_dirichlet}{Number of random Dirichlet kernels.}

\item{alpha}{Shape of the Dirichlet distribution (0.3 gives sparse
kernels; 1 is uniform on the simplex).}

\item{bin}{Width of one lag bin in the time unit of \code{unit}, used only to
label kernels (for example \verb{bin = 6, unit = "mo"} labels the six-monthly
bins "0-6 mo", "6-12 mo", ...). \code{NULL} labels bins by index.}

\item{unit}{Time unit for labels.}

\item{extra}{A named list of additional weight vectors to append, for
example a posterior-mean lag profile from a fitted model.}
}
\value{
An object of class \code{lag_kernels}: a list with one element per
kernel, each a list with \code{label}, \code{family} and \code{w}. \code{as.matrix()} returns
the weights as a matrix with one row per kernel.
}
\description{
Builds the set of lag profiles (kernels) over which the ceiling is searched
and the host-level null is evaluated. Each kernel is a vector of \code{K + 1}
non-negative weights summing to one; weight \code{k + 1} applies to the
reproductive record \code{k} bins before the first lagged bin (see
\code{\link[=convolve_lag]{convolve_lag()}} for the census convention).
}
\examples{
k <- lag_kernels(4, bin = 6, unit = "mo")
k
as.matrix(k)[1:3, ]
}
