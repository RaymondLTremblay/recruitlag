% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{regimes}
\alias{regimes}
\alias{regimes_truth}
\title{Six invented census records spanning the cases the test separates}
\format{
\code{regimes} has one row per record, unit and period (2,880 rows):
\code{record} (\code{seasonal_delayed}, \code{seasonal_gated}, \code{intermediate_delayed},
\code{intermediate_gated}, \code{aseasonal_delayed}, \code{aseasonal_gated}), \code{label},
\code{seasonality} (0, 0.5 or 1), \code{mechanism}, \code{unit}, \code{period},
\code{reproduction} and \code{recruits}. \code{regimes_truth} holds the generating
rules: the five weights of the true lag profile and the 24 gate values.
}
\description{
Constructed records, not data, in one long tibble. Three reproductive
regimes (seasonal, intermediate, aseasonal) crossed with two recruitment
mechanisms. Under \code{"delayed"}, recruits on each unit are a true delayed
consequence of that unit's own reproduction: a fixed lag profile over five
bins (weights 0.10, 0.25, 0.35, 0.20, 0.10 at one to five periods back)
with negative-binomial counts. Under \code{"gated"}, recruits arrive only at
three of the 24 periods, when an external gate is open, at a rate
proportional to each unit's average reproduction, so the record is
episodic and does not track reproduction. Each record has 20 units and 24
periods; recruits are scored at periods 6 to 24 (\code{K = 4}, \code{lag0 = 1}) and
are \code{NA} elsewhere. The generating script is \code{data-raw/regimes.R}.
}
\examples{
library(dplyr, warn.conflicts = FALSE)
r <- filter(regimes, record == "aseasonal_gated")
ce <- lag_ceiling(r, K = 4, kernels = lag_kernels(4))
ce
}
