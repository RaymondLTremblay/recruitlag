% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convolve.R
\name{convolve_lag}
\alias{convolve_lag}
\title{Apply a lag profile to a reproductive record}
\usage{
convolve_lag(X, w, t_R = NULL, lag0 = 1L, missing = c("backfill", "drop"))
}
\arguments{
\item{X}{Numeric vector: the reproductive record at periods \verb{1, 2, ...}.}

\item{w}{Numeric vector of \code{K + 1} non-negative lag weights (they are
renormalised to sum to one).}

\item{t_R}{Integer vector: the periods (indices into \code{X}) at which recruits
are scored. Defaults to the last \code{length(X) - lag0 - K} periods.}

\item{lag0}{Offset of the first bin. With \code{lag0 = 1} (the default, and the
convention of a projection matrix) weight \code{k + 1} is applied to
\code{X[t - 1 - k]}, so the first bin is the period preceding the recruit
census. With \code{lag0 = 0} the first bin is the recruit census itself.}

\item{missing}{How to treat lags that fall before the first period of \code{X}.
\code{"backfill"} (the default) reads them as \code{X[1]}, which asserts that
reproduction before the record began was at the level first observed.
\code{"drop"} returns \code{NA} for any period whose window is not fully within
the record. Zero-filling is deliberately not offered: it asserts that no
seed fell before the record began, which turns a long pure delay into a
prediction of no recruits at the first censuses and is an artefact.}
}
\value{
A numeric vector of the same length as \code{t_R}.
}
\description{
Computes the expected recruitment series implied by a set of lag weights:
at each period at which recruits are scored, the weighted sum of the
reproductive record over the preceding bins.
}
\examples{
X <- lepanthes_census$inflorescences
# a pure 24 to 30 month delay, five six-month bins, recruits scored at censuses 3 to 12
convolve_lag(X, c(0, 0, 0, 0, 1), t_R = 3:12)
}
