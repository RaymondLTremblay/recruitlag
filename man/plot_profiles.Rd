% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_profiles}
\alias{plot_profiles}
\alias{plot_expected}
\title{Lag profiles, and what they predict on the real record}
\usage{
plot_profiles(profiles, bin = NULL, unit = "", lag0 = 1L)

plot_expected(x, profiles, period = "census")
}
\arguments{
\item{profiles}{A named list of weight vectors, or a \code{lag_kernels} object.}

\item{bin, unit}{Bin width and time unit for the lag axis labels; with
\code{bin = NULL} bins are labelled by index from the first lagged bin.}

\item{lag0}{Offset of the first bin, used for the axis labels.}

\item{x}{A \code{lag_ceiling} object supplying the reproductive record, the
recruit series and the lag convention.}

\item{period}{Label for the time axis.}
}
\value{
A ggplot.
}
\description{
\code{plot_profiles()} draws a set of lag profiles as lollipops.
\code{plot_expected()} applies them to the reproductive record of a
\code{lag_ceiling} object, scales each expected series to the observed total,
and draws them over the observed recruits. The point of the pair is that
profiles which could not differ more give expected series that all but
coincide when reproduction is near-constant, and none resembles an
episodic record.
}
\examples{
pr <- list(`one interval only (projection-matrix null)` = c(1, 0, 0, 0, 0),
           `concentrated early` = c(.7, .2, .07, .03, 0),
           `evenly spread` = rep(.2, 5),
           `concentrated late` = c(0, .03, .07, .2, .7))
plot_profiles(pr, bin = 6, unit = "months")
ce <- lag_ceiling(lepanthes_census, reproduction = "inflorescences", K = 4,
                  kernels = lag_kernels(4, bin = 6, unit = "mo"))
plot_expected(ce, pr)
}
